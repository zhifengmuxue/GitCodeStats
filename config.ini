[DEFAULT]
# 默认扫描路径（请修改为你自己的项目根目录；留空时必须用 -p 参数指定）
DEFAULT_PATH =

# 默认统计天数
DEFAULT_DAYS = 1

# conda环境名称
CONDA_ENV_NAME = code_count

# 是否显示进度条
SHOW_PROGRESS = True

# Git用户名（可选，留空则使用git config中的设置）
GIT_USERNAME =

# 是否配置环境变量（True/False）
SETUP_ENV_PATH = True

# conda安装路径（可选，留空则自动检测）
CONDA_INSTALL_PATH =
